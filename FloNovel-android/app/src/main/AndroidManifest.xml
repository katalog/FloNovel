<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Font downloads and reading-position sync. Reading itself works fully offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FloNovel">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FloNovel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Receives the Dropbox OAuth redirect. The scheme is "db-<app key>", injected from
            local.properties by app/build.gradle.kts so the key is never committed.

            exported="true" is required (the browser starts it), and it is safe: the Activity does
            nothing but hand the URI to DropboxAuthSession, and an authorization code is worthless
            without the PKCE verifier, which only ever exists in this process's memory.
        -->
        <activity
            android:name=".DropboxAuthActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="${dropboxRedirectScheme}" />
            </intent-filter>
        </activity>
    </application>

</manifest>
